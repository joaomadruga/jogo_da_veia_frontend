import styled from 'styled-components'

const Logo = styled.h1`
    font-family: Pacifico;
    font-style: normal;
    font-weight: normal;
    font-size: 40px;
    margin: 0;
    width: fit-content;
    height: fit-content;
`

export default Logo;